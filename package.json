{
  "name": "fake-api-json-server",
  "version": "1.0.0",
  "description": "fake api with json-server",
  "main": "index.js",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "start": "npm run generate-data && node index.js",
    "generate-data": "node generate-data.js"
  },
  "keywords": [
    "json-server"
  ],
  "author": "phvntin",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.3"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.6.0",
    "nodemon": "^2.0.22"
  }
}
